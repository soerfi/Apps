version: '3.8'

services:
  video-downloader:
    build: .
    container_name: video-downloader
    restart: unless-stopped
    ports:
      - "5001:5001"
    volumes:
      - ./downloads:/app/downloads
    environment:
      - FLASK_ENV=production
